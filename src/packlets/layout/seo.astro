---
import { SEO } from 'astro-seo'

export interface Props {
  title?: string
  description?: string
  image?: string
  noindex?: boolean
}

const {
  title,
  description = 'Expiremental Labs of Riffy',
  image = '/cover.jpg',
  noindex = false,
} = Astro.props
const transformedTitle = title ? `${title} · Riffy Labs` : 'Riffy Labs'
---

<SEO
  title={transformedTitle}
  description={description}
  openGraph={{
    basic: {
      title: transformedTitle,
      type: 'website',
      image: image!,
    },
  }}
  twitter={{
    card: 'summary_large_image',
    title: transformedTitle,
    image,
    description,
  }}
  noindex={noindex}
/>
